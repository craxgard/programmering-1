package Inlämningsuppgifter;

class AreaOfHalfCircle {

    // Calculates the area of a half circle
    static double calculateAreaOfHalfCircle(double radius) {
        return (Math.pow(radius, 2) * Math.PI) / 2;
    }
}
